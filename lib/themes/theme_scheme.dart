import 'package:flutter/material.dart';

// Light theme custom colors
const Color kPrimaryLight = Color(0xFF99FFD6);
const Color kSecondaryLight = Color(0xFF4F878D);
const Color kTertiaryLight = Color(0xFF01241A);

// Page-specific overrides
const Color kPageBackground = Colors.white;
const Color kInkColor = Colors.black;

// Dark theme custom colors
const Color kPrimaryDark = Color.fromARGB(255, 28, 28, 28); // almost black
const Color kOnPrimaryDark = Colors.white;